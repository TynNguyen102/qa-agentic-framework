$kind: http-request
url: "{{api_base_url}}/sources?size=1"
method: GET
headers:
  Accept: application/json
  Authorization: Bearer {{access_token}}
  X-Tenant-Id: "{{tenant_id}}"
scripts:
  - type: afterResponse
    code: |-
      pm.test('List sources returns 200', function () { pm.response.to.have.status(200); });
      pm.test('Response is a JSON envelope', function () { pm.response.to.be.json; pm.expect(pm.response.json()).to.be.an('object'); });
    language: text/javascript
order: 2000
